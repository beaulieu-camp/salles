name: CI
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3


      - name: Run a one-line script
        run: ls

      - name: Run a one-line script
        run: bash calendar.sh url.csv
